{# Field template for Twitter-account #}


<div class="field__label">{{ label }}</div>

{% for item in items %}
<div class="field__item">
  <a target="_blank" href="https://twitter.com/{{ account_url }}">{{ item.content }}</a>
</div>
{% endfor %}
